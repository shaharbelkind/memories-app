{
  "name": "life-story-capsule",
  "private": true,
  "packageManager": "pnpm@9.7.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "turbo": "^2.0.6",
    "prettier": "^3.3.2"
  }
}
